# Check out every text file with LF on all platforms, overriding a Windows
# core.autocrlf=true. Shell scripts and the sed/cpp pipeline that builds
# MicroPython's qstr table from components/micropython/qstrdefsport.h both
# break on CRLF (see the Step 4 comment in components/micropython/CMakeLists.txt).
* text=auto eol=lf
