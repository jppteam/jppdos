# Name,     Type, SubType, Offset,   Size,      Flags
# 2 MB flash layout. factory is maximized; coredump dropped (coredump-to-flash
# is disabled). App must fit within factory (0x1D8000 = 1933312 bytes).
# data_fs (/data: settings, grants) and runtime_fs (/lib) hold small config only,
# so they are kept minimal to leave the app the most headroom.
nvs,        data, nvs,     0x9000,   0x6000,
phy_init,   data, phy,     0xf000,   0x1000,
factory,    app,  factory, 0x10000,  0x1D8000,
data_fs,    data, spiffs,  0x1E8000, 0x10000,
runtime_fs, data, spiffs,  0x1F8000, 0x8000,
